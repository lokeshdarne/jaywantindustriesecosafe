---
const { title, body, imgSrc, imgAlt } = Astro.props;
---

<div class="bg-brand-surface rounded-lg shadow-card border border-brand-border/50 p-6 h-full flex flex-col">
  <div class="flex justify-center mb-4">
    <img 
      src={imgSrc} 
      alt={imgAlt} 
      class="h-24 w-24 object-contain"
      loading="lazy"
    />
  </div>
  <h3 class="text-xl font-semibold mb-2 text-brand-text-primary text-center">{title}</h3>
  <p class="text-sm text-brand-text-secondary flex-grow">{body}</p>
</div>
